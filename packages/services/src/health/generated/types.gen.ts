// This file is auto-generated by @hey-api/openapi-ts

export type ClientOptions = {
    baseUrl: string;
};

export type ApiErrorResponseDto = {
    statusCode: number;
    error: string;
    /**
     * Human-readable error message or array of validation messages.
     */
    message: string | Array<string>;
    path: string;
    timestamp: string;
    /**
     * Backend trace id for this response; correlate with backend logs.
     */
    traceId: string;
};

export type HealthDatabaseStatusDto = {
    status: 'up' | 'down';
};

export type HealthResponseDto = {
    status: 'ok' | 'degraded';
    version: string;
    timestamp: string;
    uptimeSeconds: number;
    database: HealthDatabaseStatusDto;
};

export type GetHealthData = {
    body?: never;
    path?: never;
    query?: never;
    url: '/health';
};

export type GetHealthErrors = {
    /**
     * Something failed on our side. The message is masked to a generic `Internal server error` and `error` carries the exception class name — this surface publishes no closed error enum, so branch on the status rather than the value. Correlate with the `traceId` in the body. A DATABASE OUTAGE IS NOT ONE OF THESE: an unreachable database answers 200 with `status: "degraded"` and `database.status: "down"`, so a probe that treats any non-200 as dead must read the body rather than the status line. A 500 here means the process itself failed, not its dependency.
     */
    500: ApiErrorResponseDto;
};

export type GetHealthError = GetHealthErrors[keyof GetHealthErrors];

export type GetHealthResponses = {
    200: HealthResponseDto;
};

export type GetHealthResponse = GetHealthResponses[keyof GetHealthResponses];
