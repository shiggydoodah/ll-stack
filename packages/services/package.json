{
  "name": "@repo/services",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "exports": {
    "./core": "./src/core/service-result.ts",
    "./health": "./src/health/index.ts",
    "./auth": "./src/auth/index.ts",
    "./users": "./src/users/index.ts",
    "./dashboard": "./src/dashboard/index.ts"
  },
  "scripts": {
    "gen": "tsx scripts/gen-client.ts",
    "check:drift": "tsx scripts/check-client-drift.ts",
    "//build": "Deliberately absent: this package ships TypeScript source (see exports) and emits nothing. Its old build script was `tsc --noEmit` — the identical command the typecheck script still runs — so every verify checked it twice.",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "clean": "rm -rf .turbo"
  },
  "dependencies": {
    "server-only": "^0.0.1"
  },
  "devDependencies": {
    "@hey-api/openapi-ts": "catalog:",
    "@repo/config": "workspace:*",
    "@types/node": "^24.13.3",
    "@typescript/native": "catalog:",
    "tsx": "^4.23.12",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
