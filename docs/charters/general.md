# General Engineering Charter

**Placeholder — not yet written.** This file exists so the charter set has a home
for repo-wide standards that aren't specific to the backend, the frontend, or the
database. It is not policy, and nothing should cite it as such.

Until it is written, the operative rules live in:

- `AGENTS.md` — non-negotiables, path shortcuts, review scope, and what counts as
  validation.
- `docs/agents/*.agents.md` — the enforceable runbook per area.
- `eslint.config.mjs` and `pnpm verify` — the mechanically checked subset.
